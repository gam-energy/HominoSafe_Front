services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Same-origin HTTPS via tls-proxy (docker-compose.https.yml)
        NEXT_PUBLIC_API_URL: https://93.118.120.215
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: https://93.118.120.215
    restart: unless-stopped
